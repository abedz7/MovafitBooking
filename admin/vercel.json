{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps --force && GENERATE_SOURCEMAP=false SKIP_PREFLIGHT_CHECK=true TSC_COMPILE_ON_ERROR=true npm run build",
  "outputDirectory": "build",
  "installCommand": "npm install --legacy-peer-deps --force",
  "framework": "create-react-app",
  "env": {
    "GENERATE_SOURCEMAP": "false"
  }
}
