{
  "version": 2,
  "buildCommand": "npm ci && GENERATE_SOURCEMAP=false npm run build",
  "outputDirectory": "build",
  "installCommand": "npm ci --legacy-peer-deps",
  "framework": "create-react-app",
  "env": {
    "GENERATE_SOURCEMAP": "false"
  }
}
