{
  "version": 2,
  "buildCommand": "yarn install && GENERATE_SOURCEMAP=false yarn build",
  "outputDirectory": "build",
  "installCommand": "yarn install",
  "framework": "create-react-app",
  "env": {
    "GENERATE_SOURCEMAP": "false"
  }
}
